<Page
    x:Class="BoardGames.Desktop.Views.ConfirmBookingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="32" RowSpacing="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Back" Click="OnBackClicked" VerticalAlignment="Center" />
            <TextBlock
                Grid.Column="1"
                Text="Review rental request"
                FontSize="28"
                FontWeight="Bold"
                VerticalAlignment="Center" />
        </Grid>

        <Grid Grid.Row="1" ColumnSpacing="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Spacing="16">
                <Border Padding="16" CornerRadius="12" BorderBrush="#24304A" BorderThickness="1" Background="#141C2C">
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Height="90" CornerRadius="8" Background="#23304A">
                            <Image Source="{Binding GameImage}" Stretch="UniformToFill" />
                        </Border>
                        <TextBlock Grid.Column="1" Text="{Binding GameName}" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" />
                    </Grid>
                </Border>

                <Border Padding="16" CornerRadius="12" BorderBrush="#24304A" BorderThickness="1" Background="#141C2C">
                    <StackPanel Spacing="4">
                        <TextBlock Text="Date" FontWeight="SemiBold" />
                        <TextBlock>
                            <Run Text="{Binding FormattedStartDate}" />
                            <Run Text=" – " />
                            <Run Text="{Binding FormattedEndDate}" />
                        </TextBlock>
                    </StackPanel>
                </Border>

                <Border Padding="16" CornerRadius="12" BorderBrush="#24304A" BorderThickness="1" Background="#141C2C">
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0">
                            <Run Text="{Binding NumberOfDays}" />
                            <Run Text=" day(s) x " />
                            <Run Text="{Binding PricePerDay}" />
                            <Run Text=" RON/day" />
                        </TextBlock>
                        <TextBlock Grid.Column="1" Text="{Binding TotalPrice}" FontWeight="SemiBold" />
                    </Grid>
                </Border>

                <Border Padding="16" CornerRadius="12" BorderBrush="#24304A" BorderThickness="1" Background="#141C2C">
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Total" FontSize="18" FontWeight="Bold" />
                        <TextBlock Grid.Column="1" Text="{Binding TotalPrice}" FontSize="18" FontWeight="Bold" />
                    </Grid>
                </Border>
            </StackPanel>

            <StackPanel Grid.Column="1" Spacing="16" VerticalAlignment="Top">
                <Border Padding="16" CornerRadius="12" BorderBrush="#24304A" BorderThickness="1" Background="#141C2C">
                    <StackPanel Spacing="6">
                        <TextBlock Text="{Binding OwnerDisplayName}" FontWeight="Bold" FontSize="16" />
                        <TextBlock Text="{Binding City}" Foreground="#C9D2E3" />
                    </StackPanel>
                </Border>

                <Button
                    Content="Send rental request"
                    Click="OnConfirmClicked"
                    HorizontalAlignment="Stretch"
                    Style="{StaticResource AccentButtonStyle}"
                    Padding="0,12" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
