<Page
    x:Class="BoardGames.Desktop.Views.DashboardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="40">
        <StackPanel Spacing="30">
            <TextBlock Style="{StaticResource HeaderTextBlockStyle}" Text="Dashboard" />

            <StackPanel Orientation="Horizontal" Spacing="20">
                <Border Background="#333333" Padding="20" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Mesaje Noi" Style="{StaticResource CaptionTextBlockStyle}"/>
                        <TextBlock Text="{Binding NewMessagesCount}" FontSize="32" FontWeight="Bold"/>
                    </StackPanel>
                </Border>

                <Border Background="#333333" Padding="20" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Notificări" Style="{StaticResource CaptionTextBlockStyle}"/>
                        <TextBlock Text="{Binding PendingNotificationsCount}" FontSize="32" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>