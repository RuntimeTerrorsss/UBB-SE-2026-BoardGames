<UserControl
    x:Class="BoardGames.Desktop.Views.Controls.RentalDatePicker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border
        x:Name="PickerBorder"
        Padding="12"
        CornerRadius="12"
        Background="#1A2438"
        BorderBrush="#24304A"
        BorderThickness="1">
        <StackPanel Spacing="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button
                    x:Name="PrevMonthButton"
                    Grid.Column="0"
                    Content="&#x2039;"
                    Click="OnPrevMonthClick"
                    Background="Transparent"
                    BorderThickness="0"
                    Foreground="#4361EE"
                    FontSize="20"
                    Padding="8,4"
                    VerticalAlignment="Center" />

                <TextBlock
                    x:Name="MonthTitle"
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    FontWeight="Bold"
                    FontSize="16"
                    Foreground="#E8ECF4"
                    VerticalAlignment="Center" />

                <Button
                    x:Name="NextMonthButton"
                    Grid.Column="2"
                    Content="&#x203A;"
                    Click="OnNextMonthClick"
                    Background="Transparent"
                    BorderThickness="0"
                    Foreground="#4361EE"
                    FontSize="20"
                    Padding="8,4"
                    VerticalAlignment="Center" />
            </Grid>

            <Grid x:Name="WeekdayRow" />

            <Grid x:Name="DayGrid" />

            <StackPanel Orientation="Horizontal" Spacing="12">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <Border Width="12" Height="12" Background="#DC3545" CornerRadius="2" VerticalAlignment="Center" />
                    <TextBlock Text="Unavailable" FontSize="12" Foreground="#C9D2E3" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <Border Width="12" Height="12" Background="#FFC107" CornerRadius="2" VerticalAlignment="Center" />
                    <TextBlock Text="Your request" FontSize="12" Foreground="#C9D2E3" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <Border Width="12" Height="12" Background="#4361EE" CornerRadius="2" VerticalAlignment="Center" />
                    <TextBlock Text="Selected" FontSize="12" Foreground="#C9D2E3" VerticalAlignment="Center" />
                </StackPanel>
            </StackPanel>

            <TextBlock
                x:Name="SelectionLabel"
                FontSize="13"
                Foreground="#C9D2E3"
                Text="Choose a date on the calendar" />
        </StackPanel>
    </Border>
</UserControl>
