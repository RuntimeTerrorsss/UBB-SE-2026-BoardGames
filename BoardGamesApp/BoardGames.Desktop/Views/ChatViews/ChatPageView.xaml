<?xml version="1.0" encoding="utf-8" ?>
<Page
	x:Class="BoardGames.Desktop.Views.ChatViews.ChatPageView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="using:BoardGames.Desktop.Views.ChatViews"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
	mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="8">
            <Button Content="â† Back" Click="BackButton_Click" HorizontalAlignment="Left"/>
            
            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0">
                <SymbolIcon Symbol="Contact" />
                <TextBlock Text="{x:Bind ViewModel.CurrentUsername, Mode=OneWay}" VerticalAlignment="Center" FontWeight="SemiBold"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <local:LeftPanelView Grid.Column="0" />

            
            <Border
				Grid.Column="0"
				Width="1"
				HorizontalAlignment="Right"
				Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

            <local:LeftPanelView x:Name="LeftPanel" Grid.Column="0" />
            <local:RightPanelView x:Name="RightPanel" Grid.Column="1" />
        </Grid>
    </Grid>
</Page>
