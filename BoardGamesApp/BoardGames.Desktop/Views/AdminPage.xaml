<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BoardRentAndProperty.Views.AdminPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BoardRentAndProperty.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Account Administration" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center" />
            <Button Grid.Column="1" Content="Sign Out" VerticalAlignment="Center" Click="OnSignOutClicked" />
        </Grid>

        <InfoBar Grid.Row="1"
                 IsOpen="{x:Bind IsUnauthorized, Mode=OneWay}"
                 Severity="Error"
                 Title="Unauthorized"
                 Message="UM-043: You do not have permission to access the Account Administration page. Administrator role is required."
                 Margin="0,0,0,20"/>

        <InfoBar Grid.Row="1"
                 IsOpen="{x:Bind IsErrorVisible, Mode=OneWay}"
                 Severity="Error"
                 Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
                 Margin="0,0,0,20"/>

        <controls:DataGrid
            Grid.Row="2"
            ItemsSource="{x:Bind ViewModel.PagedItems, Mode=OneWay}"
            SelectedItem="{x:Bind ViewModel.SelectedAccount, Mode=TwoWay}"
            AutoGenerateColumns="False"
            Visibility="{x:Bind IsAuthorizedVisibility, Mode=OneWay}"
            x:Name="AccountsDataGrid">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="Display Name" Binding="{Binding DisplayName}" />
                <controls:DataGridTextColumn Header="Username" Binding="{Binding Username}" />
                <controls:DataGridTextColumn Header="Email" Binding="{Binding Email}" />

                <controls:DataGridTemplateColumn Header="Status">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding IsSuspended}" FontSize="12" VerticalAlignment="Center"/>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>

                <controls:DataGridTemplateColumn Header="Locked">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding IsLocked}" FontSize="12" VerticalAlignment="Center"/>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>

            </controls:DataGrid.Columns>
        </controls:DataGrid>

        <StackPanel Grid.Row="2" VerticalAlignment="Bottom" HorizontalAlignment="Right" Orientation="Horizontal" Spacing="10" Margin="0,10,20,10"
                    Visibility="{x:Bind IsAuthorizedVisibility, Mode=OneWay}">
            <Button Content="Suspend Account" Command="{x:Bind ViewModel.SuspendAccountCommand}" />
            <Button Content="Unsuspend Account" Command="{x:Bind ViewModel.UnsuspendAccountCommand}" />
            <Button Content="Reset Pass" Click="OnResetPasswordClicked" />
            <Button Content="Unlock" Command="{x:Bind ViewModel.UnlockAccountCommand}" />
        </StackPanel>

        <Grid Grid.Row="3" Margin="0,20,0,0" Visibility="{x:Bind IsAuthorizedVisibility, Mode=OneWay}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{x:Bind ViewModel.ShowingText, Mode=OneWay}" VerticalAlignment="Center" />
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10">
                <Button Content="Prev" Command="{x:Bind ViewModel.PreviousPageCommand}" />
                <TextBlock Text="Page" VerticalAlignment="Center" />
                <TextBlock Text="{x:Bind ViewModel.CurrentPage, Mode=OneWay}" VerticalAlignment="Center" FontWeight="Bold"/>
                <TextBlock Text="of" VerticalAlignment="Center" />
                <TextBlock Text="{x:Bind ViewModel.PageCount, Mode=OneWay}" VerticalAlignment="Center" />
                <Button Content="Next" Command="{x:Bind ViewModel.NextPageCommand}" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>