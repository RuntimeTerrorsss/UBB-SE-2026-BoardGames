<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="BoardGames.Desktop.Views.NotificationsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:notificationDtoNamespace="using:BoardGames.Shared.DTO"
      mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="My Notifications" FontSize="28" FontWeight="SemiBold" Margin="12"/>

        <ListView x:Name="ItemsListView" Grid.Row="1"
                  ItemsSource="{Binding PagedItems}"
                  SelectionMode="None"
                  IsItemClickEnabled="False">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="notificationDtoNamespace:NotificationDTO">
                    <Border Margin="12,0,12,10" Padding="15" BorderBrush="LightGray" BorderThickness="1" CornerRadius="8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Spacing="4">
                                <TextBlock Text="{x:Bind Title}" FontSize="16" FontWeight="SemiBold"/>
                                <TextBlock Text="{x:Bind Body}" Opacity="0.8" TextWrapping="Wrap"/>
                                <TextBlock Text="{x:Bind TimeDisplay}" FontSize="12" Opacity="0.6"/>
                            </StackPanel>
                            <Button Grid.Column="1"
                                    Click="DeleteButton_Click"
                                    ToolTipService.ToolTip="Delete"
                                    Width="40"
                                    Height="40">
                                <SymbolIcon Symbol="Delete" />
                            </Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Grid Grid.Row="2" Margin="12">
            <TextBlock Text="{Binding ShowingText}" VerticalAlignment="Center" HorizontalAlignment="Left"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Previous" Click="PrevButton_Click" Margin="4"/>
                <TextBlock Text="{Binding CurrentPage}" VerticalAlignment="Center" Margin="12,0"/>
                <TextBlock Text="/" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="{Binding PageCount}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <Button Content="Next" Click="NextButton_Click" Margin="4"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
