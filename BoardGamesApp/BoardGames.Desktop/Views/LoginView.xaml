<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BoardGames.Desktop.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BoardGames.Desktop.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Width="600" Padding="40" Spacing="0" HorizontalAlignment="Center" VerticalAlignment="Center">

            <TextBlock Text="Sign in" FontSize="28" FontWeight="Bold" Margin="0,0,0,10"/>

            <InfoBar x:Name="ErrorBar" IsOpen="False" Severity="Error" Margin="0,0,0,16"/>

            <StackPanel Spacing="4" Margin="0,0,0,20">
                <TextBlock Text="Username/Email" FontSize="13" FontWeight="SemiBold"/>
                <TextBox Text="{Binding Identifier, Mode=TwoWay}" PlaceholderText="Enter username or email"/>
                <TextBlock x:Name="ErrorText" Foreground="Red" FontSize="11" Visibility="Collapsed"/>
            </StackPanel>

            <StackPanel Spacing="4" Margin="0,0,0,12">
                <TextBlock Text="Password" FontSize="13" FontWeight="SemiBold"/>
                <PasswordBox x:Name="PasswordInput" PlaceholderText="Enter password"/>
                <TextBlock x:Name="PasswordErrorText" Foreground="Red" FontSize="11" Visibility="Collapsed"/>
            </StackPanel>

            <Button Content="Sign in" x:Name="LoginButton" Command="{Binding LoginCommand}" HorizontalAlignment="Stretch" Margin="0,10,0,0"/>

            <HyperlinkButton Command="{Binding GoToRegisterCommand}" Content="Don't have an account? Create one" HorizontalAlignment="Stretch" Margin="0,12,0,0"/>

        </StackPanel>
    </ScrollViewer>
</Page>
